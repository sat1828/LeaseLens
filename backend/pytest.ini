[pytest]
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning:passlib.*
    ignore::DeprecationWarning:bcrypt.*
markers =
    integration: marks tests as integration tests requiring a live database (deselect with -m 'not integration')
